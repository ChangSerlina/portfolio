services:
  frontend:
    build: .
    container_name: portfolio-vue
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - app_shared_network

networks:
  app_shared_network:
    external: true